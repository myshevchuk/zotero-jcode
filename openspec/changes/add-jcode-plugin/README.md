# add-jcode-plugin

Convert journal_code.scrpt into a Zotero 7+ plugin (.xpi) with context-menu and keyboard-shortcut triggers, bundled TSV with override preference, and a TDD-driven pure-logic core.
